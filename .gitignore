node_modules
dist
.env
.env.local
# Any local env override (.env.development.local, .env.production.local). These hold
# keys and per-machine overrides and must never be committed.
.env*.local
.DS_Store

# Editor / tooling local config
.claude/
.vscode/
.idea/

# Tauri build artifacts (recreated on every build)
src-tauri/target/
src-tauri/gen/

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Supabase CLI scratch state (link cache, downloaded binaries). Recreated
# on the fly by `supabase link` / `supabase functions deploy`.
supabase/.temp/
